{
    "name": "eco-bot",
    "version": "1.0.0",
    "description": "BotBuilder v4 TypeScript empty bot sample",
    "author": "Microsoft",
    "license": "MIT",
    "main": "./lambda.js",
    "scripts": {
        "build": "npm run lint && tsc --build",
        "lint": "tslint -c tslint.json '*.ts' && tslint -c tslint.json 'helpers/**/*.ts'",
        "start": "tsc --build && node ./lambda.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com"
    },
    "dependencies": {
        "aws-serverless-express": "^3.3.8",
        "botbuilder": "^4.10.4",
        "express": "^4.21.2"
    },
    "devDependencies": {
        "tslint": "^6.1.3",
        "typescript": "^4.0.5"
    }
}
